{
  "name": "backend",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.0",
    "nodemon": "^3.1.10"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "npm-check-updates": "^18.0.3",
    "supertest": "^6.3.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "jest --coverage",
    "check-modules": "ncu --peer --interactive --no-deprecated --format group"
  }
}
